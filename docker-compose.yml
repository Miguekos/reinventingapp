# docker-compose.yml
version: "2"
services:
  webapp_envios:
#    image: quasarenvios:1.0
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    expose:
      - "80"
    # volumes:
    # - /Users/kbs/git/peladonerd/varios/1/www:/usr/share/nginx/html:ro
    # - //d/storeDocker/html:/usr/share/nginx/html:ro
    environment:
      - VIRTUAL_HOST=tuenvioexpress.apps.com.pe,www.tuenvioexpress.apps.com.pe
      - LETSENCRYPT_HOST=tuenvioexpress.apps.com.pe,www.tuenvioexpress.apps.com.pe
      - LETSENCRYPT_EMAIL=kevin.silva.095@gmail.com
networks:
  default:
    external:
      name: nginx-proxy_default
